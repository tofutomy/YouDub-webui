fastapi
uvicorn[standard]
python-dotenv
pydantic
python-multipart
openai
yt-dlp[default]
dora-search>=0.1.12
diffq>=0.2.1
julius>=0.2.3
lameenc>=1.2
openunmix
openai-whisper
spacy
voxcpm
soundfile
pydub
librosa
audiostretchy
numpy
# FunASR and VoxCPM depend on HuggingFace Transformers APIs that are still
# safest on the 4.x line. Keep vLLM-only dependency churn out of the Windows
# application environment.
transformers>=4.36.2,<5
pytest
httpx[socks]

# Qwen3-ASR: local speech recognition with transformers backend (Windows-compatible).
qwen-asr

# Optional: FunASR vLLM inference engine (Fun-ASR-Nano / GLM-ASR-Nano / LLMASR).
# Fun-ASR-Nano's standard AutoModel path raises
# ``NotImplementedError: batch decoding is not implemented``; install vllm
# and set FUNASR_USE_VLLM=auto/on to enable.
# Requirements: NVIDIA GPU with >= 8 GB VRAM, CUDA >= 11.8.
# Native Windows is not a supported vLLM runtime; use WSL2/Linux/Docker or
# configure FUNASR_REMOTE_BASE_URL to call a separate FunASR/vLLM service.
# vllm>=0.10.0
