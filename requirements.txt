fastapi
uvicorn[standard]
python-dotenv
pydantic
python-multipart
openai
yt-dlp[default]
dora-search>=0.1.12
diffq>=0.2.1
julius>=0.2.3
lameenc>=1.2
openunmix
openai-whisper
spacy
voxcpm
soundfile
pydub
librosa
audiostretchy
numpy
pytest
httpx[socks]

# Optional: FunASR vLLM inference engine (Fun-ASR-Nano / GLM-ASR-Nano / LLMASR).
# Fun-ASR-Nano's standard AutoModel path raises
# ``NotImplementedError: batch decoding is not implemented``; install vllm
# and set FUNASR_USE_VLLM=auto/on to enable.
# Requirements: NVIDIA GPU with >= 8 GB VRAM, CUDA >= 11.8.
# Windows: pick a vllm wheel matching the installed CUDA runtime.
# vllm>=0.10.0
